# Entorno del proyecto — Python 3.12
# El interprete del sistema (3.14) no tiene ruedas de torch ni stable-baselines3.
#   python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- Numerico ---
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3

# --- Control ---
control==0.10.1          # python-control: ss, c2d, dlqr, ctrb, obsv
slycot==0.6.0            # rutinas SLICOT; mejora la resolucion de Riccati (opcional)

# --- Plataforma TCLab ---
tclab==1.0.0
pyserial==3.5

# --- Aprendizaje por refuerzo ---
gymnasium==1.0.0
stable-baselines3==2.4.0
torch==2.5.1

# --- Figuras ---
matplotlib==3.9.2

# --- Configuracion y utilidades ---
pyyaml==6.0.2
tqdm==4.67.1

# --- Calidad de codigo ---
black==24.10.0
ruff==0.8.4
pytest==8.3.4
